info:
  name: Listar Recursos
  type: http
  seq: 1

http:
  method: GET
  url: "{{server.url}}/resources"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
