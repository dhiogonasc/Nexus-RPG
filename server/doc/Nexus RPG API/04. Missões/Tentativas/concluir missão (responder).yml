info:
  name: concluir missão (responder)
  type: http
  seq: 4

http:
  method: POST
  url: "{{server.url}}/attempts/{{attempt.id}}/finish"
  body:
    type: json
    data: |-
      [
        {
          "questionId": 7,
          "alternativeId": 13
        },
        {
          "questionId": 8,
          "alternativeId": 15
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
