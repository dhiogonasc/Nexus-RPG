info:
  name: concluir missão (responder)
  type: http
  seq: 4

http:
  method: POST
  url: "{{server.url}}/attempts/{{attempt.id}}/finish"
  body:
    type: json
    data: |-
      [
        {
          "questionId": 1,
          "alternativeId": 1
        },
        {
          "questionId": 2,
          "alternativeId": 3
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
