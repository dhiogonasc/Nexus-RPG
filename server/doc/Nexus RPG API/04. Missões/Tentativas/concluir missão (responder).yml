info:
  name: concluir missão (responder)
  type: http
  seq: 4

http:
  method: POST
  url: "{{server.url}}/attempts/{{attempt.id}}/finish"
  body:
    type: json
    data: |-
      [
        {
          "questionId": 3,
          "alternativeId": 5
        },
        {
          "questionId": 4,
          "alternativeId": 7
        }
      ]
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
