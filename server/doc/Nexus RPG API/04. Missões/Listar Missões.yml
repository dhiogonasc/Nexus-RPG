info:
  name: Listar Missões
  type: http
  seq: 1

http:
  method: GET
  url: "{{server.url}}/missions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
