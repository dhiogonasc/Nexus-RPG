info:
  name: Detalhes do Planeta
  type: http
  seq: 2

http:
  method: GET
  url: "{{server.url}}/planets/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
